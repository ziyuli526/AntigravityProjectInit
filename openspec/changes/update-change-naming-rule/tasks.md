## 1. Project Naming Rule Updates

- [ ] 1.1 Update `openspec` validation logic to require `^\d{2}-[a-z0-9-]+$` for `new change` command
- [ ] 1.2 Implement auto-increment logic to find max change prefix in `openspec/changes` directory and prepend next number

## 2. Testing and Documentation

- [ ] 2.1 Update validation unit tests for new naming rules
- [ ] 2.2 Rebuild OpenSpec docs referencing change naming conventions
